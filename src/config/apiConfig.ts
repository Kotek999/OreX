import { API_KEY, EXCHANGE_RATES_API_KEY, HISTORICAL_DATA_API_KEY } from "@env";

export const apiConfig = {
  apiKey: API_KEY,
  exchangeRates: {
    apiKey: EXCHANGE_RATES_API_KEY,
  },
  historicalData: {
    apiKey: HISTORICAL_DATA_API_KEY,
  },
};
